extends partial/base

block site_title
    != page.title + " · " + config.title

block container
    include mixins/post
    +post(page)

block pagination
    include mixins/paginator
    +paginator_post()
